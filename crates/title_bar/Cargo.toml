[package]
name = "title_bar"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
license = "GPL-3.0-or-later"

[lints]
workspace = true

[lib]
path = "src/title_bar.rs"
doctest = false

[features]
default = []

test-support = [
    "call/test-support",
    "client/test-support",

    "gpui/test-support",

    "project/test-support",
    "remote/test-support",
    "util/test-support",
    "workspace/test-support",
    "recent_projects/test-support",
]

[dependencies]
agent_settings.workspace = true
anyhow.workspace = true
fs.workspace = true
platform_title_bar.workspace = true
chrono.workspace = true
client.workspace = true
cloud_api_types.workspace = true
command_palette_hooks.workspace = true
db.workspace = true

git_ui_core.workspace = true
gpui.workspace = true
icons.workspace = true
project.workspace = true
recent_projects.workspace = true
remote.workspace = true
remote_connection.workspace = true
rpc.workspace = true
semver.workspace = true
schemars.workspace = true
serde.workspace = true
settings.workspace = true
smallvec.workspace = true
telemetry.workspace = true
theme.workspace = true
ui.workspace = true
util.workspace = true
workspace.workspace = true
zed_actions.workspace = true
arrayvec = "0.7.6"

# Collaboration (calls, channels, LiveKit) and the updater are desktop-only: the browser
# build uses its shared-workspace presence and host-driven update UI instead.
[target.'cfg(not(target_family = "wasm"))'.dependencies]
auto_update.workspace = true
call.workspace = true
channel.workspace = true
gpui = { workspace = true, features = ["screen-capture"] }
livekit_client.workspace = true

[dev-dependencies]
call = { workspace = true, features = ["test-support"] }
client = { workspace = true, features = ["test-support"] }
gpui = { workspace = true, features = ["test-support"] }
project = { workspace = true, features = ["test-support"] }
recent_projects = { workspace = true, features = ["test-support"] }
remote = { workspace = true, features = ["test-support"] }
rpc = { workspace = true, features = ["test-support"] }
semver.workspace = true
settings = { workspace = true, features = ["test-support"] }
util = { workspace = true, features = ["test-support"] }
workspace = { workspace = true, features = ["test-support"] }
